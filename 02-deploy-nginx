# Lab 2: Deploy Nginx to Kubernetes

##  Objective

Deploy a simple NGINX web server to a Kubernetes cluster using YAML manifests.  
This lab builds on Lab 1 and introduces the use of Deployment and Service resources.

---

## Files

- `nginx-deployment.yaml` – Defines the NGINX Deployment with 2 replica


---

## How to Deploy

```bash
# Apply the Deployment
kubectl apply -f deployment.yaml

# Check the status
kubectl get pods
kubectl get svc
